services:
  - type: web
    name: stock-simulator
    runtime: docker
    region: oregon
    plan: free
    envVars:
      - key: ALLOWED_ORIGINS
        value: https://${RENDER_SERVICE_NAME}.onrender.com,http://localhost:3000
